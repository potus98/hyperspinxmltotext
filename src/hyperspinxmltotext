import os
import sys
import xml.dom.minidom

def main():
	# use the parse() function to load and parse an XML file
	doc = xml.dom.minidom.parse("example.xml");

	# print out the document node and a field
	print (doc.nodeName)
	print (doc.field.tagName)


if __name__ == "__main__":
	main();